const styles = StyleSheet.create({
  tagSelectorContainer: {
    width: '100%',
    marginTop: 12,
    marginBottom: 28, // Distinct, clean layout spacing before mode rows
    paddingHorizontal: 4,
    marginVertical: 18,
  },
  tagSelectorTitle: {
    fontSize: 12,
    fontWeight: '700',
    color: '#E5E7EB', // Crisp light-gray accessible typography
    textTransform: 'uppercase',
    marginBottom: 12,
    letterSpacing: 1.2,
  },
  tagSelectorRow: {
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
  },

   // 🌟 THE CHIP WRAPPER CONTAINER: Gives each animated chip a flexible width share
    chipWrapper: {
      flex: 1,
    },

  // 🌟 THE CONTAINER CARD BOX STYLING:
    tagChip: {
      width: '100%',
      height: 42,
      backgroundColor: '#111113', // Matches your homepage widget base card token
      borderRadius: 8,
      borderWidth: 1,
      borderColor: '#1E1E22',
      flexDirection: 'row',       // Places the icon and text next to each other
      alignItems: 'center',
      justifyContent: 'center',
      paddingHorizontal: 8,
    },

  tagChipInactive: {
    borderColor: '#374151',      // Explicit dark charcoal visibility boundaries
    backgroundColor: '#1F2937',  // Medium background block contrast
  },
  chipIcon: {
    marginRight: 6, // Snug structural spacing separating icon art from plain string
  },
  tagChipText: {
    fontSize: 14,
    color: '#A3A3A3',
    letterSpacing: 0.3,
  },
  tagChipTextInactive: {
    color: '#D1D5DB',            // High visibility color fallback for web layouts
    fontWeight: '600',
  },

   captureHero: {
      width: '100%',
      // 🌟 30% SCREEN VIEWPORT HEIGHT: Scales perfectly across all device monitors
      height: Platform.OS === 'web' ? '30vh' : SCREEN_HEIGHT * 0.3,
      backgroundColor: 'transparent', // FLOATING EFFECT: Blends banner edge borders cleanly
      position: 'relative',
      borderRadius: 14,
      overflow: 'hidden',
      marginTop: 14,
      marginBottom: 14,
    },

    captureImage: {
      width: '100%',
      height: '100%',
      opacity: 1.0, // Full bright high-contrast color visibility
    },

  captureImageOverlay: {
      ...StyleSheet.absoluteFillObject,
      backgroundColor: 'rgba(17, 17, 19, 0.05)', // Minimal tint filter layer to keep banner very bright
    },

    captureHeroCopy: {
      position: 'absolute',
      left: 16,
      bottom: 16,
      zIndex: 4, // Forces layout text tracking layers above graphic blocks
    },

    captureEyebrow: {
        fontSize: 10,
        fontWeight: '700',
        color: '#00f0ff', // System Vivid Cyber Cyan
        letterSpacing: 1.5,
        textTransform: 'uppercase',
      },

    captureTitle: {
        fontSize: 24,
        fontWeight: '800',
        color: '#FFFFFF',
        marginTop: 4,
        // 🌟 LINE HEIGHT PROTECTION: Gives the 3-line layout breaks breathing room
        lineHeight: 30,
      },

      captureHeroContainer: {
         width: '100%',
         height: 180,
         backgroundColor: '#000000',
         overflow: 'hidden',
       },
       backgroundImageCanvas: {
         width: '100%',
         height: '100%',
         // ✅ THE FIX: Uses flexbox alignment instead of absolute coordinates
         // to position text perfectly at the bottom without cutting it off.
         justifyContent: 'flex-end',
       },
       imageOverlayMask: {
         ...StyleSheet.absoluteFillObject,
         backgroundColor: 'rgba(17, 17, 19, 0.25)',
       },
       heroCopyLayoutBlock: {
         paddingLeft: 24,
         paddingBottom: 24,
         zIndex: 3,
       },
       captureEyebrow: {
         fontSize: 10,
         fontWeight: '700',
         color: '#00f0ff',
         letterSpacing: 1.5,
         textTransform: 'uppercase',
       },
       captureTitle: {
         fontSize: 22,
         fontWeight: '800',
         color: '#FFFFFF',
         marginTop: 6,
         lineHeight: 28,
       },

       primaryButton: {
         backgroundColor: '#00ffcc', // Assuming your original bright active color
         transform: [{ scale: 1 }],
         borderWidth: 1,
         borderColor: 'transparent',
         // ... your other layout rules
       },
       scanningActiveButton: {
         backgroundColor: '#0a0a0f',       // Swaps background to dark cyber canvas
         borderColor: '#00ffcc',           // Gives it a glowing neon accent border perimeter
         borderWidth: 1,
         transform: [{ scale: 0.97 }],     // Squeezes button slightly to show active compression engagement
         shadowColor: '#00ffcc',           // Active neon ambient drop glow mapping
         shadowOffset: { width: 0, height: 0 },
         shadowOpacity: 0.4,
         shadowRadius: 10,
         elevation: 5,
       },
       scanningText: {
         color: '#00ffcc',                 // Flips the text from dark to neon glowing electric blue
         letterSpacing: 1.5,               // Spreads typography for matrix data theme look
         fontWeight: '900',
       },
       modalOverlayContainer: {
         backgroundColor: '#16161a',       // Soft dark grey background panel instead of total pitch black
         borderRadius: 12,
         padding: 20,
         borderWidth: 1,
         borderColor: '#26262b',
         marginVertical: 16,
       },
       formFieldBlock: {
         marginBottom: 16,                 // Disperses text rows cleanly
       },
       inputLabelText: {
         color: '#00ffcc',                 // Glowing cyber emerald labels to call out metrics contexts
         fontSize: 11,
         fontWeight: '700',
         letterSpacing: 1.2,
         marginBottom: 6,
       },
       modalEditableInputField: {
         backgroundColor: '#1e1e24',       // Raised visible canvas card for typing
         borderColor: '#00ffcc',           // Active highlighted accent indicator perimeter
         borderWidth: 1,
         borderRadius: 8,
         color: '#ffffff',                 // High-contrast clean white text
         padding: 14,
         fontSize: 14,
         minHeight: 50,
         textAlignVertical: 'top',
       },
       modalInputField: {
         backgroundColor: '#1a1a1f',       // Recessed surface box
         borderColor: '#2d2d34',
         borderWidth: 1,
         borderRadius: 8,
         color: '#e4e4e7',                 // High contrast text
         padding: 14,
         fontSize: 14,
         minHeight: 60,
         textAlignVertical: 'top',
       },
       disabledReadOnlyField: {
         backgroundColor: '#111114',       // Sinks back deeper into screen background hierarchy
         borderColor: '#202024',
         color: '#8a8f98',                 // Dims typography to convey a locked state cleanly
         opacity: 0.7,
       },
       modalButtonsRow: {
         flexDirection: 'row',
         justifyContent: 'space-between',
         gap: 12,
         marginTop: 10,
       },
       cancelBtn: {
         flex: 1,
         backgroundColor: '#202024',
         borderRadius: 8,
         paddingVertical: 16,
         alignItems: 'center',
       },
       cancelBtnText: {
         color: '#ffffff',
         fontWeight: 'bold',
         fontSize: 15,
       },
       saveBtn: {
         flex: 1,
         backgroundColor: '#00ffcc',       // Bright green action indicator
         borderRadius: 8,
         paddingVertical: 16,
         alignItems: 'center',
       },
       saveBtnText: {
         color: '#000000',
         fontWeight: 'bold',
         fontSize: 15,
       }


});
